# MongoDB connection string (Mongoose) - reuses DATABASE_URL if MONGODB_URI is unset
DATABASE_URL="mongodb://ipk_admin:<password>@<atlas-host>/ipkwealth_crm_test?retryWrites=true&w=majority"

# Server
PORT=4000

# Allowed frontend origin(s) for CORS (comma separated for multiple).
FRONTEND_URL=https://lead.ipkwealth.com
